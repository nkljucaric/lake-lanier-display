// Copy this file to secrets.h (gitignored, never committed) and fill in
// real credentials there.

#pragma once

struct WifiNetwork
{
  const char *ssid;
  const char *password;
};

// One entry per network the device should be able to join. WiFiMulti
// tries all of them at boot and connects to whichever is in range.
// Add the in-laws' network here once it's known — no code changes needed.
static const WifiNetwork WIFI_NETWORKS[] = {
    {"your-home-network-ssid", "your-home-network-password"},
    // {"in-laws-network-ssid", "in-laws-network-password"},
};
